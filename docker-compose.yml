services:
  # ==========================================
  # 1. MAIN BACKEND (Your original code)
  # ==========================================
  tickframe:
    build: .
    ports:
      - "8080:8000"
    env_file:
      - .env

  # ==========================================
  # 2. MACHINE LEARNING MICROSERVICE
  # ==========================================
  ml-service:
    build: ./ml_service
    container_name: ml_service_container
    ports:
      - "8001:8001"
    volumes:
      - ./ml_service/logs:/code/logs
    restart: always